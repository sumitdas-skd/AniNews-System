window.CONFIG = {
    // When deployed to Vercel, this must point to your Railway backend URL.
    // The api-interceptor.js only applies this on non-localhost environments.
    API_BASE_URL: 'https://aninews.up.railway.app'
};
